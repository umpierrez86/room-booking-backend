services:
  db:
    image: postgres:16
    environment: { POSTGRES_PASSWORD: postgres, POSTGRES_DB: rooms }
    ports: ["5432:5432"]
  backend:
    build: .
    env_file: .env
    ports: ["8000:8000"]
    depends_on: [db]
